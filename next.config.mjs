/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    MONGODB_URI:
      "mongodb+srv://todoapp:todoapp123@react-todo.164ai.mongodb.net/todo-app",
  },
};

export default nextConfig;
